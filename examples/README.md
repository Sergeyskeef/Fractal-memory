# 📚 Examples - Fractal Memory System

Практические примеры использования системы.

---

## 📂 Список примеров

### 01. Базовое использование (`01_basic_usage.py`)

**Что демонстрирует:**
- Инициализация FractalMemory
- Сохранение информации (remember)
- Извлечение информации (recall)
- Консолидация памяти между уровнями (L0 → L1 → L2/L3)
- Получение статистики памяти

**Когда запускать:**
После реализации `src/core/memory.py` (Phase 2)

**Запуск:**
```bash
python examples/01_basic_usage.py
```

---

### 02. Самообучение (`02_learning_demo.py`)

**Что демонстрирует:**
- Логирование опыта (успехи и ошибки)
- Извлечение стратегий из опыта
- Получение рекомендаций для новых задач
- Обновление confidence стратегий
- Статистика обучения

**Когда запускать:**
После реализации `src/core/learning.py` (Phase 3)

**Запуск:**
```bash
python examples/02_learning_demo.py
```

---

## 🚀 Быстрый старт

### 1. Подготовка инфраструктуры

```bash
# Запустить Docker контейнеры
make docker-up

# Выполнить миграции
make migrate

# Проверить что всё работает
make smoke
```

### 2. Настроить .env

```bash
cp .env.example .env
# Отредактировать .env (установить пароли, API ключи)
```

### 3. Установить зависимости

```bash
# С Poetry (рекомендуется)
poetry install

# Или с pip
pip install -r requirements.txt
```

### 4. Реализовать код

Примеры — это **шаблоны**. Код в них закомментирован до реализации:

1. Реализуй `src/core/memory.py` (см. `docs/03_PHASE2_MEMORY.md`)
2. Раскомментируй код в `01_basic_usage.py`
3. Запусти пример

---

## 📋 Статус реализации

| Пример | Требует | Статус |
|--------|---------|--------|
| `01_basic_usage.py` | `src/core/memory.py` | ⏳ Шаблон готов, ждёт код |
| `02_learning_demo.py` | `src/core/learning.py` | ⏳ Шаблон готов, ждёт код |

---

## 🔧 Troubleshooting

### Ошибка: "ModuleNotFoundError: No module named 'src'"

```bash
# Убедись что запускаешь из корня проекта
cd /path/to/fractal_memory_updated
python examples/01_basic_usage.py

# Или добавь в PYTHONPATH
export PYTHONPATH="${PYTHONPATH}:$(pwd)"
```

### Ошибка: "Could not connect to Neo4j"

```bash
# Проверь что контейнеры запущены
docker ps | grep fractal-memory

# Если нет — запусти
make docker-up

# Проверь логи
make docker-logs
```

### Ошибка: "NEO4J_PASSWORD not set"

```bash
# Создай .env файл
cp .env.example .env

# Отредактируй .env и установи пароль
nano .env
```

---

## 🎓 Следующие шаги

После запуска примеров:

1. **Изучи метрики** — смотри сколько токенов экономится
2. **Эксперименты** — меняй параметры (L0/L1 capacity, importance threshold)
3. **Свои сценарии** — создай `03_custom_example.py` для твоих задач
4. **Production** — см. `docs/05_PHASE4_PRODUCTION.md` для деплоя

---

## 📚 Дополнительные ресурсы

- **Документация**: `docs/` — поэтапная документация
- **Тесты**: `tests/` — unit и integration тесты
- **Исходники**: `src/` — реализация системы

---

## ❓ Вопросы?

Если примеры не работают:

1. Проверь `make status` — статус инфраструктуры
2. Проверь `make smoke` — smoke test
3. Посмотри troubleshooting в конце каждого Phase документа

**Примеры работают как демонстрация. Для production используй полную реализацию из `src/`.**
