# 07. Настройка разработки на ВМ Timeweb

## 🎯 Цель

Настроить удалённую ВМ Timeweb для разработки Fractal Memory System с подключением через SSH и VS Code/Cursor Remote.

**Время**: 30-60 минут  
**Результат**: Готовая к работе ВМ с SSH доступом и настроенным окружением

---

## 📋 Чек-лист настройки

- [ ] ВМ Timeweb создана (2 CPU, 6GB RAM, Ubuntu)
- [ ] SSH доступ настроен
- [ ] Docker и Docker Compose установлены
- [ ] Проект скопирован на ВМ
- [ ] VS Code/Cursor Remote SSH настроен
- [ ] Контейнеры запущены и работают

---

## 1️⃣ Подготовка ВМ на Timeweb

### Создание ВМ

1. Войди в панель управления Timeweb
2. Создай новую ВМ с параметрами:
   - **ОС**: Ubuntu 20.04 или 22.04
   - **CPU**: 2 ядра
   - **RAM**: 6GB
   - **Диск**: 20GB+ (рекомендуется 40GB)
   - **Сеть**: публичный IP адрес

3. Запиши:
   - IP адрес ВМ
   - Логин (обычно `root` или `ubuntu`)
   - Пароль (или SSH ключ)

---

## 2️⃣ Настройка SSH

### Первое подключение

```bash
# Подключись к ВМ
ssh root@YOUR_VM_IP
# или
ssh ubuntu@YOUR_VM_IP

# Если используешь пароль, введи его при запросе
```

### Настройка SSH ключа (рекомендуется)

На локальной машине:

```bash
# Создай SSH ключ (если ещё нет)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Скопируй публичный ключ на ВМ
ssh-copy-id root@YOUR_VM_IP
# или
ssh-copy-id ubuntu@YOUR_VM_IP

# Теперь можно подключаться без пароля
ssh root@YOUR_VM_IP
```

### Настройка SSH на ВМ (опционально)

```bash
# На ВМ: отредактируй /etc/ssh/sshd_config
sudo nano /etc/ssh/sshd_config

# Рекомендуемые настройки:
# PermitRootLogin yes  # или no, если используешь другого пользователя
# PasswordAuthentication no  # отключить пароли, только ключи
# PubkeyAuthentication yes

# Перезапусти SSH
sudo systemctl restart sshd
```

---

## 3️⃣ Установка Docker и Docker Compose

### Автоматическая установка (рекомендуется)

Используй скрипт из проекта:

```bash
# На ВМ
cd ~
wget https://raw.githubusercontent.com/your-repo/fractal_memory_updated/main/scripts/setup_vm.sh
chmod +x setup_vm.sh
./setup_vm.sh
```

### Ручная установка

#### Docker

```bash
# Обновить пакеты
sudo apt update
sudo apt upgrade -y

# Установить зависимости
sudo apt install -y \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# Добавить официальный GPG ключ Docker
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Добавить репозиторий Docker
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установить Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Проверить установку
docker --version
# Должно быть: Docker version 20.10 или выше
```

#### Настройка пользователя для Docker (без sudo)

```bash
# Создать группу docker (если ещё нет)
sudo groupadd docker

# Добавить текущего пользователя в группу
sudo usermod -aG docker $USER

# Применить изменения (выйти и войти снова, или):
newgrp docker

# Проверить
docker ps
# Должно работать без sudo
```

#### Docker Compose

```bash
# Docker Compose уже установлен как плагин (docker compose)
# Проверить версию
docker compose version
# Должно быть: Docker Compose version 2.0 или выше

# Если нужна отдельная установка (старая версия):
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
docker-compose --version
```

---

## 4️⃣ Перенос проекта на ВМ

### Способ 1: Через Git (рекомендуется)

```bash
# На ВМ
cd ~
git clone https://github.com/your-repo/fractal_memory_updated.git
cd fractal_memory_updated

# Или если репозиторий приватный:
git clone git@github.com:your-repo/fractal_memory_updated.git
```

### Способ 2: Через SCP (если нет Git)

На локальной машине:

```bash
# Создать архив проекта
cd /path/to/fractal_memory_updated
tar -czf fractal_memory.tar.gz --exclude='.git' --exclude='__pycache__' --exclude='*.pyc' .

# Скопировать на ВМ
scp fractal_memory.tar.gz root@YOUR_VM_IP:~/

# На ВМ: распаковать
ssh root@YOUR_VM_IP
cd ~
tar -xzf fractal_memory.tar.gz
cd fractal_memory_updated
```

### Настройка .env файла

```bash
# На ВМ
cd ~/fractal_memory_updated
cp .env.example .env
nano .env

# Заполни:
# NEO4J_PASSWORD=your_secure_password
# OPENAI_API_KEY=sk-your-key-here
# Остальные настройки можно оставить по умолчанию
```

---

## 5️⃣ Настройка VS Code Remote SSH

### Установка расширения

1. Открой VS Code
2. Перейди в Extensions (Ctrl+Shift+X)
3. Найди и установи: **Remote - SSH**

### Подключение к ВМ

1. Нажми F1 (или Ctrl+Shift+P)
2. Введи: `Remote-SSH: Connect to Host`
3. Выбери: `Add New SSH Host`
4. Введи: `root@YOUR_VM_IP` (или `ubuntu@YOUR_VM_IP`)
5. Выбери файл конфигурации SSH (обычно `~/.ssh/config`)
6. Нажми F1 снова → `Remote-SSH: Connect to Host` → выбери свой хост
7. VS Code подключится к ВМ и откроет окно

### Работа с проектом

1. В VS Code на ВМ: File → Open Folder
2. Выбери: `/root/fractal_memory_updated` (или `/home/ubuntu/fractal_memory_updated`)
3. Теперь можно работать с файлами как на локальной машине

### Установка расширений на ВМ

VS Code предложит установить расширения на удалённой машине:
- Python
- Docker
- YAML
- И другие по необходимости

---

## 6️⃣ Настройка Cursor Remote SSH

Cursor поддерживает Remote SSH так же, как VS Code:

1. Открой Cursor
2. Установи расширение **Remote - SSH** (если доступно)
3. Подключись к ВМ так же, как в VS Code

**Примечание**: Если Cursor не поддерживает Remote SSH напрямую, можно:
- Использовать VS Code для редактирования
- Или работать через терминал + локальный редактор с синхронизацией через Git

---

## 7️⃣ Запуск проекта на ВМ

### Проверка ресурсов

```bash
# На ВМ
cd ~/fractal_memory_updated
bash scripts/check_vm_resources.sh

# Должно показать:
# ✅ RAM: 6GB (достаточно)
# ✅ CPU: 2 ядра (достаточно)
# ✅ Диск: достаточно места
# ✅ Docker: установлен
```

### Запуск контейнеров

```bash
# На ВМ
cd ~/fractal_memory_updated

# Запустить контейнеры
docker compose up -d

# Проверить статус
docker compose ps

# Должно быть:
# NAME                    STATUS
# fractal-memory-neo4j    healthy
# fractal-memory-redis    healthy
```

### Выполнение миграций

```bash
# Установить зависимости Python (если ещё не установлены)
python3 -m pip install -r requirements.txt

# Или через Poetry
poetry install

# Запустить миграции
python migrations/run_migrations.py
```

### Smoke test

```bash
# Проверить что всё работает
python scripts/smoke_test.py

# Должно показать:
# ✅ Neo4j: PASS
# ✅ Redis: PASS
```

---

## 8️⃣ Мониторинг ресурсов

### Проверка использования ресурсов

```bash
# Мониторинг контейнеров в реальном времени
docker stats

# Проверка использования диска
df -h

# Проверка использования памяти
free -h

# Проверка нагрузки CPU
top
# или
htop  # если установлен
```

### Если ресурсов не хватает

Если видишь проблемы:
- Neo4j падает с OutOfMemory
- Система тормозит
- Контейнеры перезапускаются

**Решение**: Увеличь ресурсы ВМ в панели Timeweb:
- RAM: 6GB → 8GB или 12GB
- CPU: 2 → 4 ядра

После изменения перезапусти ВМ и контейнеры.

---

## 9️⃣ Доступ к сервисам

### Neo4j Browser

Если нужно открыть Neo4j Browser с локальной машины:

```bash
# На локальной машине: настроить SSH туннель
ssh -L 7474:localhost:7474 root@YOUR_VM_IP

# Затем открой в браузере: http://localhost:7474
```

Или используй VS Code Port Forwarding:
1. В VS Code Remote SSH
2. Порт 7474 → Forward Port
3. Открой http://localhost:7474

### Redis

Аналогично для Redis (порт 6379):

```bash
ssh -L 6379:localhost:6379 root@YOUR_VM_IP
```

---

## 🔧 Troubleshooting

### Не могу подключиться по SSH

```bash
# Проверить что SSH сервис запущен на ВМ
sudo systemctl status sshd

# Проверить файрвол
sudo ufw status
# Если включен, разрешить SSH:
sudo ufw allow 22/tcp
```

### Docker требует sudo

```bash
# Добавить пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Проверить
docker ps
```

### Контейнеры не запускаются

```bash
# Проверить логи
docker compose logs

# Проверить что порты свободны
sudo netstat -tulpn | grep -E '7474|7687|6379'

# Если порты заняты, измени в docker-compose.yml
```

### Нехватка памяти

```bash
# Проверить использование
free -h
docker stats

# Если памяти мало:
# 1. Увеличь ресурсы ВМ в Timeweb
# 2. Или временно уменьши настройки в docker-compose.yml
#    (но это ограничит функциональность)
```

---

## ✅ Критерии завершения

- [ ] SSH подключение работает
- [ ] Docker и Docker Compose установлены
- [ ] Проект скопирован на ВМ
- [ ] VS Code/Cursor Remote SSH настроен
- [ ] Контейнеры запущены и healthy
- [ ] Миграции выполнены
- [ ] Smoke test пройден

---

## 📚 Следующие шаги

После настройки ВМ:

1. Начни разработку через VS Code Remote SSH
2. Следи за использованием ресурсов
3. При необходимости увеличь ресурсы ВМ
4. Продолжай по документации: [03_PHASE2_MEMORY.md](03_PHASE2_MEMORY.md)

---

## 🔗 Полезные ссылки

- [Timeweb Документация](https://timeweb.cloud/docs/)
- [VS Code Remote SSH](https://code.visualstudio.com/docs/remote/ssh)
- [Docker Documentation](https://docs.docker.com/)
- [Neo4j Documentation](https://neo4j.com/docs/)

